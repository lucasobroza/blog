<div id="new">
	<p>
	  <strong>Title:</strong>
	  <%= @post.title %>
	</p>
	 
	<p>
	  <strong>Text:</strong>
	  <%= @post.text %>
	</p>
	 
	<h2>Comentários:</h2>
	<%= render @post.comments %>
	 
	<h4>Comente!</h4>
	<%= render "comments/form" %>
	 
	<%= link_to 'Editar Post', edit_post_path(@post) %> |
	<%= link_to 'Outros Posts', posts_path %>
</div>